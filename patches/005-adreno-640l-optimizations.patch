# Adreno 640L Performance & FPS Optimizations for SM7325
diff --git a/drivers/gpu/drm/msm/adreno/a6xx/a6xx_gpu.c b/drivers/gpu/drm/msm/adreno/a6xx/a6xx_gpu.c
index 0000000..1111111 100644
--- a/drivers/gpu/drm/msm/adreno/a6xx/a6xx_gpu.c
+++ b/drivers/gpu/drm/msm/adreno/a6xx/a6xx_gpu.c
@@ -120,12 +120,18 @@
 	/* Aumenta frequência mínima para reduzir stutter */
 	.devfreq.min_freq = 180000000,
 	.devfreq.max_freq = 670000000,     // Max da 640L
+	.idle_timeout = 80,                // Reduzido para FPS mais estável
 };

+/* Boost para jogos */
+static struct adreno_boost_param adreno_640l_boost = {
+	.boost = 1,
+	.boost_freq = 550000000,
+};

 static const struct adreno_gpu adreno_gpu_640l = {
 	.gpu = {
 		.pwrlevel = 0,
-		.min_pwrlevel = 4,
+		.min_pwrlevel = 2,   // Começa em frequência mais alta
 		.max_pwrlevel = 0,
 	},
+	.boost_param = &adreno_640l_boost,
 };
